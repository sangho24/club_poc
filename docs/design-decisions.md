# 디자인 결정 기록

`src/theme.ts` 는 값과 그 값을 고른 짧은 이유만 갖는다. 왜 그 방향으로 갔는지의 서사는 여기 있다.
토큰 파일에 문단이 쌓이면 코드로 읽히지 않는다.

읽는 순서: 아래로 갈수록 최근이다.

---

## 2026-08-11 · 다크 네이비에서 흰 바탕으로 뒤집었다

처음에는 다크 네이비 위에 오렌지가 발광하는 톤으로 잡았다. 구단 앱의 성격(팬덤 미디어)에는
맞았지만 **읽는 화면으로는 실패했다.**

심화 스탯·확률 근거처럼 글자가 많은 화면에서 어두운 면은 대비를 벌기 어렵다. 그리고 오렌지가
넓게 쓰이면 그 자체가 소음이 되어 정작 봐야 할 수치가 묻힌다.

**그래서 흰 바탕이 기본이고, 구단 색은 보조다.** 기존 KBO 앱(`kbo_poc`)이 확립한 iOS 그룹 리스트
문법을 그대로 따른다 - `#F2F2F7` 배경 + 흰 카드 + 작은 회색 섹션 머리글, 모서리 24·14·999, 평평한 카드.

구단 앱임을 드러내는 것은 **면적이 아니라 자리다.** 오렌지는 브랜드 마크·선택 상태·핵심 수치·
게이지에만 놓고, 본문과 카드는 무채색이 가져간다. 같은 규칙을 KBO 앱은 네이비로, 이 앱은
오렌지로 쓰는 것이라 두 앱은 여전히 한눈에 구분된다.

### 딸려 나온 규칙 둘

**섹션 머리글은 작은 회색이다.** 초판은 18pt 굵은 검정이었는데, 그러면 화면을 내릴 때마다 굵은
제목이 연달아 나와 **화면 전체가 제목 대결**이 된다. 그 균일한 반복이 곧 "만들다 만 화면"처럼
보이는 원인이다. 머리글이 물러나야 흰 카드가 시각 단위로 읽힌다.

**목록의 항목마다 카드를 띄우지 않는다.** 같은 라운드·같은 패딩의 흰 상자가 끝없이 반복되면
어디가 묶음이고 어디가 낱개인지 구분이 사라진다. 묶이는 것은 카드 하나 안의 행으로 넣는다.

---

## 2026-08-25 · 토큰 레이어 재설계 (S2)

`HeroUI` 71개 컴포넌트 실측과 `WWiT` 캡처 101장 판독을 근거로 축을 다시 세웠다.
갭 분석은 [`design-gap.md`](./design-gap.md) 에 있다.

### 1. 틴트를 파생 함수로 바꿨다 — 그리고 15% 가 아니라 10% 다

`brandSoft` `liveSoft` `winSoft` `warnSoft` 네 개가 각각 하드코딩되어 있었다.
역산해 보니 **원저자는 이미 사실상 같은 규칙을 쓰고 있었다.**

| 토큰        | 값        | 역산한 알파                 |
| ----------- | --------- | --------------------------- |
| `brandSoft` | `#FFEDE5` | 약 10.2%                    |
| `winSoft`   | `#E8F3ED` | 약 10%                      |
| `liveSoft`  | `#FBE9ED` | 약 9%                       |
| `warnSoft`  | `#F6EEDC` | **7~15% (채널마다 제각각)** |

규칙은 있었지만 코드에 적혀 있지 않았다. 그래서 색을 하나 더 만들 때마다 손으로 틴트를
만들어야 했고, `warnSoft` 처럼 규칙에서 벗어난 값이 섞여도 아무도 몰랐다.

**HeroUI 를 그대로 따르지 않은 이유.** HeroUI 의 `danger-soft` 를 실측하니 `색상 / 0.15` 였다.
그 값을 우리 색에 적용하면 위에 얹는 글자 대비가 이렇게 된다.

```
알파   brand  live   win    warn   최저    AA(4.5)
15%    4.33   4.27   4.32   4.39   4.27    ❌
13%    4.46   4.43   4.46   4.51   4.43    ❌
11%    4.58   4.60   4.56   4.63   4.56    ✅
10%    4.62   4.66   4.66   4.71   4.62    ✅   ← 채택
```

15% 를 쓰면 **접근성이 후퇴한다.** 10% 는 기존 하드코딩 값과 같은 수준을 유지하면서
네 개를 규칙 하나로 대체하고, 어긋나 있던 `warnSoft` 를 교정한다.

**남는 제약: 알파는 뒤에 무엇이 있는지를 탄다.** 위 수치는 전부 흰 카드 기준이다.
회색 지면(`colors.bg`) 위에 얹으면 3.9:1 로 떨어진다.
**틴트 배지는 카드 안에만 둔다** - 이 규칙을 어기면 대비가 조용히 무너진다.

### 2. 항목을 구분만 하는 색을 신설했다

기존 색은 전부 의미를 갖는다 - 브랜드·진행중·승·경고·신뢰도. 구종 비율이나 타구 방향처럼
**의미는 없고 서로 구별만 되면 되는** 자리에 쓸 색이 하나도 없었다.
승을 뜻하는 초록으로 '슬라이더'를 칠할 수는 없다.

```
라이트  #0A78D4  #00A08C  #7A45E0  #D1257E  #3E9B22
다크    #2E86DB  #0FA88A  #8B60E2  #DB4A8F  #4FAE33
기타    #98A0AB
```

**오렌지 인접 hue 를 통째로 비웠다.** 구단 색이 오렌지라 카테고리에 오렌지가 섞이면
선택 상태와 혼동된다.

**눈으로 고르지 않았다.** OKLab 기준 6개 검사(명도 밴드·채도 하한·색각이상 분리·정상시각 하한·
지면 대비)를 돌렸다. 첫 후보는 탈락했다.

```
[FAIL] Chroma floor         #14897A(0.098) #6E7581(0.02) — 회색으로 읽힘
[FAIL] Normal-vision floor  #14897A↔#1E7FBF ΔE 12.2 — 색각 이상이 아니어도 구분 어려움
```

hue 를 벌리고 회색을 슬롯에서 빼서 통과시켰다. 회색은 카테고리가 아니라 '기타' 전용이다.

**다섯에서 멈춘다.** 여섯 번째 항목은 색을 새로 만드는 게 아니라 `other` 로 접는다.
색을 늘리는 순간 위 검사가 깨진다.

### 3. 굵기를 다섯 단계에서 세 단계로 줄였다

`400 · 500 · 600 · 700 · 800` 을 쓰고 있었다. 단계가 다섯이면 무엇이 더 중요한지 눈이
판단하지 못한다. **500 과 800 을 버렸다.**

| 토큰        | 지금 → 제안 | 이유                                                     |
| ----------- | ----------- | -------------------------------------------------------- |
| `metric`    | 800 → 700   | 수치는 크기로 이미 강하다. 굵기까지 최대면 소리를 지른다 |
| `cardTitle` | 700 → 600   | 화면에서 가장 굵은 자리를 수치에 넘긴다                  |
| `caption`   | 500 → 400   | 500 은 400 과 구분되지 않으면서 단계만 늘렸다            |

2026-08-11 에 머리글을 작은 회색으로 물린 것과 같은 문제였다. 머리글은 물러났는데
**카드 안쪽에서 같은 제목 대결이 다시 벌어지고 있었다.**

HeroUI 는 UI 전반이 500 하나인데, 한글은 500 이 얇게 뭉개져 그대로 쓸 수 없어 400/600/700 으로 갔다.

### 4. 컨트롤에 크기 축을 세우고 primary 버튼을 만들었다

버튼이 `ExternalButton`(앱 밖으로 나가는 링크)과 `SecondaryButton`(회색 보조) 둘뿐이었다.
**가장 중요한 행동을 담을 버튼이 없어서** 그 자리를 카드 전체 터치가 대신 메우고 있었다.
카드를 누르면 뭔가 일어나지만 무엇이 일어나는지는 눌러 봐야 안다.

```
sm 36   md 44   lg 52
```

**HeroUI 는 32/36/40 인데 따르지 않았다.** 그건 마우스로 누르는 웹 기준이다.
손가락 최소 터치는 44 이고 `spacing.touchMin` 에 이미 그렇게 적혀 있었다.
구조는 HeroUI 를 따르되 수치는 모바일로 옮긴 자리다.

### 5. 게이지가 새는 구멍을 막았다

`Gauge` 의 `tone` 이 `string` 이었다. 화면이 원시 값을 직접 넘기고 있었다.

```ts
// PlayersScreen.tsx:306, 377
<Gauge position={pt.usage} tone={colors.neutralFill} />
<Gauge position={pos} markerAt={midPos} tone={above ? colors.brand : colors.neutralFill} />
```

토큰 체계 밖으로 새는 구멍이라, 새 토큰을 만들어도 화면이 그냥 우회한다.
`gaugeFill` 맵과 `GaugeTone` union 으로 막았다. 벗어나면 컴파일이 멈춘다.

### 6. 상태를 정의했다

`disabled` 와 `loading` 을 받는 컴포넌트가 하나도 없었다. 눌림만 있고
**'지금은 누를 수 없다'와 '기다리는 중이다'를 말할 방법이 없었다.**

`disabled` 는 0.38 이다. 0.5 는 아직 눌릴 것처럼 보인다.

---

## 2026-08-25 · 컴포넌트 적용 (S3)

### 7. 굵기 누수 28곳 - S2 가 눈에 띄지 않았던 이유

`typography` 토큰의 굵기를 세 단계로 줄였는데 화면이 거의 그대로였다. 세어 보니
**토큰을 거치지 않고 `fontWeight` 를 직접 쓰는 자리가 28곳**이었다.

```
전  700×24  800×17  600×17  500×11  400×2
후  700×41  600×17  400×13
```

`Gauge` 의 `tone: string` 과 정확히 같은 종류의 누수다. 토큰을 고쳐도 화면이 우회한다.
**토큰 레이어를 바꾸는 작업은 반드시 사용처 전수 조사를 동반해야 한다.**

### 8. 봉인이 실제로 잡았다

`GaugeTone` union 을 넣자 컴파일러가 두 곳을 세웠다.

```
PlayersScreen.tsx(306,46): Type 'string' is not assignable to type
  '"brand" | "warn" | "neutral" | "good" | "bad" | undefined'
PlayersScreen.tsx(377,49): ...
```

306 행은 **구종 사용률**이었다. 직구·슬라이더·커브는 의미가 아니라 구별이므로
카테고리 팔레트가 들어갈 자리다. 전에는 전부 같은 회색이라 막대만 봐서는 어느 줄이
어느 구종인지 알 수 없어 이름을 매번 다시 읽어야 했다.

다만 봉인을 뚫지 않았다. 화면은 색이 아니라 **슬롯 번호만** 넘긴다.

```tsx
<Gauge position={pt.usage} category={i} />
```

`categoryColor(i)` 가 5개를 넘으면 `other` 로 접으므로 "다섯에서 멈춘다"는 규칙이
코드에서 저절로 지켜진다.

### 9. 구간 게이지가 잘못된 신호를 하나 교정했다

377 행(심화 지표 게이지)을 `RangeGauge` 로 바꿨더니 값이 달라졌다.

```
전   wRC+ 오렌지    wOBA 오렌지    BABIP 오렌지     ← 셋 다 "평균 초과"
후   wRC+ 상위      wOBA 상위      BABIP 평균권
```

BABIP .320 은 리그 평균 .310 보다 높지만 **정상 범위 안**이다. 예전 게이지는 평균을
넘었다는 이유만으로 wRC+ 126 과 똑같은 구단 색을 칠했는데, 실제로는 노이즈 수준
차이였다. 구간이 그것을 구분한다.

밴드 경계는 기존 `low·mid·high` 세 지점의 중간이다 - **없던 리그 기준을 지어내지 않았다.**
`gaugePosition` 이 방향을 이미 정규화해 둬서(1 이 언제나 좋은 쪽 끝, ERA 처럼 낮을수록
좋은 지표도 마찬가지) 상위·하위로 읽을 수 있다.

### 10. 부팅 빈 화면

`boot === 'loading'` 이 `<SafeAreaView />` 하나만 반환하고 있었다. 빈 화면은 '오는 중'이
아니라 '아무것도 없음'으로 읽혀서 앱이 죽은 것처럼 보인다. 홈 화면과 같은 리듬의
자리표시자를 세웠다.

⚠ 웹에서는 저장소 읽기가 마이크로태스크 하나라 **사실상 보이지 않는다.**
타입체크와 코드로만 확인했고 실기기 AsyncStorage 에서 잠깐 나타날 것이다.

---

## 아직 하지 않은 것

**다크모드는 열어만 두고 켜지 않았다.** `categorical.dark` 는 들어갔지만 나머지 색은
라이트 전용이다. 토큰 구조가 다크를 받을 수 있게 되었다는 뜻이지 지금 동작한다는 뜻이 아니다.

**`Button` 을 쓰는 화면이 아직 없다.** 컴포넌트는 만들었고 `SecondaryButton` 이 내부적으로
쓰지만, `primary` 가 필요한 자리를 화면별로 정하는 것은 S4 다.
"이 화면의 주된 행동이 무엇인가"는 디자인 판단이라 기계적으로 넣을 수 없다.

**남은 신설 컴포넌트**: 해석 배너(P6 - 사실과 해석이 같은 옷을 입고 있다) ·
행 끝 판정 텍스트 · 범례 행 · 근거 항목이 순차로 켜지는 로딩.

---

## 2026-08-25 · 실서비스 관점 리뷰 반영 (S4)

진단: **이 앱에는 앱이 두 개 겹쳐 있었다.** 팬이 쓰는 앱과 파트너에게 보여주는 제안서가
같은 지면을 쓰고 있었다. 섹션 제목만 봐도 드러난다 - "어떤 구조인가" "운영 원칙" "계산".

### 11. 상단바가 고정 문자열에 자리를 내주고 있었다

"대전 한화생명 볼파크"는 바뀌지 않는 값인데 매 화면 반복하며 가장 비싼 자리를 썼다.
로고와 텍스트가 양끝으로 벌어져 로고가 브랜드가 아니라 **레이블**처럼 읽히기도 했다.
그 자리를 변하는 값이 가져간다 - 멤버십 등급, 안 읽은 알림.

알림함(`src/notifications.ts`)을 함께 만들었다. 온보딩이 알림을 받겠느냐 물어 놓고
**받은 알림을 볼 자리가 없었다.** 끈 종류는 걸러 숨기는 게 아니라 애초에 도착하지 않는다 -
회색으로 남아 있으면 "껐는데 왜 오지?"가 되어 설정을 못 믿는다.

### 12. 주차 - 축을 시간에서 의사결정으로

`180/120/90/60분` 칩이 세 가지로 틀렸다. ① 간격에 기준이 없다 ② 앱은 경기 시각도 현재
시각도 안다(고르는 값이 아니라 아는 값) ③ 팬의 질문은 "지금 출발하면 어디에 댈 수 있나"다.

주차는 셋 중 하나를 포기하는 선택이라(거리·요금·출차) 정렬 기준을 그 셋으로 뒀다.
`PARKING_BASIS` 로 근거를 화면에 적는다 - 추정치를 실시간인 척 보여주면 빗나갔을 때
팬이 화면 전체를 안 믿는다. 심화 지표에 표본 신뢰도를 붙인 것과 같은 원칙이다.

**실물 확인에서 잡은 것**: 저녁에 열면 전부 만차라 정렬이 무의미해진다.
그때는 정렬 기준을 감추고 "주차는 어렵다, 대중교통을 보라"고 답한다.
고를 수 없는 것을 고르라고 내미는 화면이 되면 안 된다.

### 13. 스폰서 - 지면이 아니라 순간을 판다

P7 이 전략인데 구현이 `presenter` 한 줄뿐이라 시연에서 수익 구조가 안 보였다.

두 곳에 넣었다. 자리를 고른 기준은 **맥락 일치**다.

- 라이브 `결정적 순간` - 레버리지 최고점 타석 **바로 아래** 한 번만. 매 행마다 붙이면
  광고 지면이지 순간이 아니고 팬은 피드를 못 읽는다. presented by **한화생명**
- 직관 `주차` - 자동차보험 브랜드와 주차 안내는 맥락이 맞는다. 팬이 "지금 어디에 댈까"를
  묻는 자리에 자동차 브랜드가 있으면 광고가 아니라 후원으로 읽힌다.
  presented by **한화손해보험**

전면 광고(인터스티셜)는 쓰지 않는다. 팬덤 앱에서 화면을 가로막는 광고는 구단에 대한
감정까지 깎는다.

### 14. 후원의 집 - 제안서를 걷어내고 물음표로

"어떤 구조인가"(협찬금 모델·전북 현대 벤치마크), "운영 원칙" 전체, 배분 구조 각주를 삭제했다.
팬의 질문은 "우리 동네에 이글스 후원 가게가 있나, 가면 뭘 주나" 하나다.
다만 처음 보는 이름이라 한 번은 물어보므로 `InfoTip` 이 그 질문만 받는다.
등급 배지(플래그십·공식·등록)는 제휴가 얼마나 단단한지를 말하는 정보라 남겼다.

제휴 가게 상호는 전부 가상이다. 동네 자영업자라 실명을 쓰면 오히려 문제가 된다.

### 15. 움직임 - 선수 폼 루프 · 굿즈 회전 전시

팬덤 미디어라면서 선수 화면에 움직이는 것이 없었고, 커머스라면서 굿즈 탭이 전부 글자였다.
실제 경기 영상은 KBO·구단·중계사가 권리를 나눠 가져 PoC 에 넣을 수 없어, 확보한 사진에
느린 팬(pan)을 입혀 자리와 리듬만 증명한다.

⚠ **첫 구현에서 얼굴이 잘렸다.** `resizeMode="cover"` 로 채웠더니 세로 중앙이 남아 다리만
보였다. `PlayerAvatar` 주석에 이미 같은 경고가 있었는데 읽고도 반복했다.
폭을 채우고 높이는 `aspectRatio` 에 맡겨 **위를 기준으로** 자른다.
**확대는 쓰지 않는다** - 이미지가 상자보다 훨씬 커서 중심 기준 scale 은 조금만 줘도
보이는 구간이 크게 밀린다.

### 16. 진단 오류 정정 - 라이브 문자중계는 이미 있었다

리뷰 초판은 "라이브에 중계가 없다"고 했으나 틀렸다. 섹션 제목만 템플릿 리터럴
(``title={`${s.inning}회${halfLabel} 문자중계`}``)이라 `SectionTitle title="` grep 에서 빠졌다.
**문자열 리터럴만 세는 검색으로 화면 구성을 판단한 것이 원인이다.**
작업을 '피드 신설'이 아니라 '피드 안에 스폰서 순간 끼워넣기'로 좁혔다.

---

## 2026-08-26 · 지면을 구단 색 쪽으로 기울였다 (S5)

### 17. 흰 모드가 PoC 처럼 보인 이유는 카드가 아니라 지면이었다

`#F2F2F7` + 흰 카드는 iOS `systemGroupedBackground` 기본값 그대로다. 문제는 그 값이
**파란 회색**(OKLCh hue 286)이라는 데 있었다. 구단 색은 오렌지(hue 37)인데 지면이
정반대쪽으로 식어 있으면, 오렌지는 칩과 게이지에 점점이 떠 있을 뿐 **화면 어디에도
브랜드가 묻지 않는다.** "기본값을 그대로 쓴 화면"으로 읽히는 실체가 이것이었다.

회색 계열 전체를 구단 오렌지의 hue 로 돌렸다.

| 토큰                       | 전                    | 후                    |
| -------------------------- | --------------------- | --------------------- |
| `bg`                       | `#F2F2F7`             | `#F8F1EF`             |
| `surface`                  | `#F4F5F7`             | `#FAF3F1`             |
| `raised`                   | `#E9EBF0`             | `#F0E9E7`             |
| `border`                   | `#E8EAEE`             | `#EFE8E6`             |
| `borderStrong`             | `#D6DAE1`             | `#DFD8D6`             |
| `dim`                      | `#E2E5EA`             | `#EAE3E1`             |
| `skeleton.base` / `.sheen` | `#E4E7EC` / `#EEF0F3` | `#ECE5E3` / `#F5EEEC` |

**L(명도)은 한 토큰도 건드리지 않았다.** hue 만 돌리고 채도를 `C=0.008` 로 통일했다.
L 을 함께 움직이면 면 위계(카드 > 지면 > 눌림)와 글자 대비가 동시에 흔들려 무엇 때문에
달라졌는지 알 수 없게 된다. 산출과 회귀 검증은 `tools/tint-ground-final.js` 가 한다.

**카드는 순백으로 남겼다.** 지면이 따뜻해야 흰 카드가 떠오른다. 카드까지 기울이면
둘 다 같은 온도가 되어 대비가 사라진다.

`C=0.008` 인 이유: 원래 `bg` 의 채도가 0.0066 이었다. **강도는 원저자가 쓰던 만큼 두고
방향만 반대로 돌린 것**이다. 실측한 후보표에서 0.012 이상은 살구빛이 뚜렷해져 지면이
스스로를 주장했고, 0.004 이하는 흰 카드 옆에서 사라졌다.

### 18. 지면색이 세 곳에 복제되어 있었다

`#F2F2F7` 이 `src/theme.ts` · `app.json` · `tools/postexport-pwa.js` 에 각각 리터럴로
있고 서로를 참조하지 않았다. 여기에 `TabBar.tsx` 가 `rgba(244,245,247,0.82)` 로
**`surface` 의 알파 변형을 손으로 적어** 네 번째 복제본을 만들고 있었다.

넷 중 하나만 놓쳐도 그 자리에서만 파란 회색이 튄다. `Gauge.tone: string` · 굵기 누수
28곳과 같은 종류의 누수다. **토큰을 바꾸는 작업은 반드시 사용처 전수 조사를 동반한다**는
S3 의 교훈이 또 필요했다.

웹 초기 페인트도 함께 막았다. 번들이 마운트되기 전까지 브라우저가 자기 기본 흰색을
칠하는데, 지면이 흰색에서 멀어질수록 그 한 프레임이 흰 섬광으로 보인다.

### 19. 발견한 기존 결함 - 섹션 머리글이 지면 위에서 AA 미달

`mutedText`(`#6E7581`, 13pt/600)가 **흰 카드 위에서는 4.64:1 로 통과하지만 지면 위에서는
4.16:1** 이다. 섹션 머리글만 지면 위에 놓이는데 `theme.ts` 주석은 "흰 카드 위에서 세 단계
모두 AA 를 넘는다"고만 적혀 있어 이 자리가 검사 밖에 있었다.

이번 변경이 만든 문제가 아니고(전 4.16 → 후 4.16) 악화시키지도 않았다. 다만 고치려면
`mutedText` 를 `#686F7B` 까지 내려야 하는데, 그러면 캡션·크레딧까지 함께 진해져
"물러나는 자리"라는 성격이 바뀐다. **머리글 전용 색을 하나 더 만들지, 머리글을 카드 안으로
들일지는 별도 판단이라 이번에 하지 않았다.**

### 20. 라이브가 난잡했던 이유 - 알림 목록을 지면에 그대로 부었다

`liveAlerts` 는 **푸시 알림용**이다. 앱 밖에서 한 줄만 보고도 상황을 알 수 있게 문장을
통째로 담는다. 화면이 그 목록을 그대로 렌더하고 있어서, 같은 수치가 한 화면에 세 번
나왔다.

```
현재 상황 카드   기대득점 1.47점 · 레버리지 4.29
지금 눈여겨볼 것  "이 타석 하나가 평균 타석의 4.3배… 현재 기대득점은 1.47점입니다"
                 ↑ 바로 위 카드가 이미 말한 것을 문장으로 되풀이
matchup 알림     title=pred.headline · body=pred.reasons[0]
                 ↑ '이 승부' 카드의 제목·첫 근거와 **글자 그대로 같다**
```

화면에는 **화면이 아직 말하지 않은 것만** 남겼다. 엔진은 손대지 않았다 - 알림으로 나갈
때는 그 문장들이 그대로 필요하다.

**정보가 많아서 난잡한 게 아니라 같은 말이 카드를 하나 더 차지해서 난잡했다.**

함께 정리한 것:

- **베이스·볼카운트 카드를 '이 승부' 카드의 머리로 넣었다.** 둘 다 같은 한 가지(지금 이
  타석)를 말하는데 카드가 갈려 있으면 레버리지 4.29 라는 수치가 매치업과 떨어져서
  "그래서 이게 왜 중요한가"가 보이지 않는다.
- **스폰서 모멘트의 틴트 면을 왼쪽 띠로 바꿨다.** `brandSoft` 로 면을 통째로 깔았더니
  중계 행이 흐르다가 색 덩어리가 끼어들어 **광고가 중계보다 시선을 먼저 가져갔다.**
  자리는 그대로 명확한데 오렌지 면적은 크게 준다. "브랜드는 면적이 아니라 자리"가
  여기에도 적용된다.

---

---

## 2026-08-26 · 중계다운 중계 · 클릭되는 동선 · 표를 산 뒤 (S6)

세 가지를 채웠다. 라이브에 라인업, 직관의 일정 → 예매 연결, 문자중계 확장.
그리고 셋을 붙이는 동안 **기존 데이터가 서로를 반박하는 자리 네 곳**이 드러났다.

### 21. 화면을 하나 붙이면 없던 결함이 생기는 게 아니라, 있던 결함이 보인다

라인업 카드는 `roster.ts` 를 그리기만 하면 되는 일로 보였다. 그런데 카드를 세우고
바로 아래 문자중계를 보니 **둘이 다른 경기를 말하고 있었다.**

```
선발 오더    1 이원석 · 2 페라자 · 3 문현빈 · 4 강백호 · 5 노시환 …
문자중계     문현빈(3번) → 채은성(6번) → 노시환(5번) → 페라자(2번) → 최재훈 → 김태연
                                                              ↑ 선발 아홉에 없는 백업 둘
```

`PLATE_SEQUENCE` 의 타순이 처음부터 어긋나 있었다. 라인업이 화면에 없을 때는 아무도
몰랐고, **명단을 그리는 순간 그 카드가 고발자가 된다.**

같은 일이 세 번 더 있었다.

| 무엇                      | 어떻게 드러났나                                                              |
| ------------------------- | ---------------------------------------------------------------------------- |
| 9회말 주자·아웃이 튄다    | 문자중계를 펼치니 **적히지 않은 일이 두 번 일어나야** 만루가 되는 흐름이었다 |
| 일정의 요일이 하루씩 밀림 | 달력 격자에 얹으니 **월요일(휴식일)에 경기가 서 있었다**                     |
| 라인스코어가 안 움직인다  | 9회말까지 넘겨도 8회말 칸이 비어 있고, 첫 타석인데 안타가 이미 7개였다       |

넷 다 **표시 방식을 바꾸자 드러난 데이터 결함**이지 새로 만든 버그가 아니다.
그래서 이번 작업의 절반은 화면이 아니라 데이터를 고치는 일이었다.

교훈은 S3 의 "토큰 레이어를 바꾸는 작업은 사용처 전수 조사를 동반한다"와 같은 모양이다 -
**같은 사실을 말하는 화면을 하나 더 세우는 작업은 데이터 정합 검사를 동반한다.**

### 22. 세는 값은 중계 한 곳에만 둔다

`LINESCORE` 상수를 지웠다. 이닝별 점수·안타·실책은 이제 문자중계에서 합산한다
(`src/liveFeed.ts` 의 `lineScoreAt(step)`).

손으로 적어 둔 표와 마흔 행짜리 중계가 한 화면에 같이 있으면 **언제든 서로를 반박할 수
있다.** 3회말에 2점이 찍혀 있는데 그 이닝 중계에 득점이 없으면, 팬은 어느 쪽이 맞는지
알 수 없고 그때부터 이 화면의 다른 수치도 안 믿는다.

`roster.ts` 가 타율을 적어 두지 않고 `sabermetrics` 가 계산하는 것과 같은 규칙이다.
검사도 같이 세웠다 - `verify-stats.ts` 가 이닝 득점 합 · 총점 · 타순 연속성을 기계로 센다.

```
✓ 18개 하프이닝 · 득점 4:3 · 안타 9:8 · 실책 0:1 · 타순 연속
```

### 23. 규칙이 글로만 적혀 있으면 그건 규칙이 아니다

예매 오픈 시각이 `TICKET_OPEN_AT = '2026-08-14T14:00'` 상수 하나였다. 그 옆에
`openRule: '홈경기 기준 7일 전 오후 2시 오픈'` 이라는 **글**이 따로 있었고, 둘은
서로를 참조하지 않았다.

일정이 눌리지 않을 때는 그걸로 충분했다. 경기를 고를 수 있게 되는 순간 무너진다 -
어느 경기를 눌러도 같은 날짜를 말하면 그건 규칙이 아니라 장식이다. 값을 규칙에서 뽑았다
(`ticketOpenAt(game)`). 요일도 같은 이유로 계산으로 옮겼다(`weekdayOf`).

### 24. 배지는 다수가 아니라 예외에 단다

일정 목록에서 예매가 열린 경기에 `예매 중` 배지를 달았는데, 오픈 규칙을 실제로 계산하고
보니 **가까운 홈경기는 거의 다 열려 있다.** 여섯 줄 중 다섯 줄에 같은 배지가 서면
그건 배지가 아니라 배경이다.

뒤집었다 - 아직 못 사는 경기만 `8.18 오픈` 이라고 말한다. 눈이 찾아야 하는 것은
"다 되는 것들" 사이에 섞인 **안 되는 하나**다.

### 25. 라인업을 2열로 접은 이유

이 화면의 규칙 ②는 "화면의 뼈대는 문자중계이고 예측은 그 위에 얹힌 한 장"이다.
아홉 줄짜리 명단을 그 사이에 세우면 예측과 중계가 화면 하나만큼 벌어져 규칙이 깨진다.
라인업은 **한 번 확인하고 마는 참고 지면**이라 자리를 많이 가져가면 안 된다.

2열 다섯 줄로 접고, 남는 열 번째 칸에 선발투수를 넣었다. 빈 칸이 사라지는 동시에
라인업 카드가 실제로 갖춰야 할 정보가 갖춰진다.

**정적인 명단으로 두지 않았다.** 현재 타석을 표시하면 이 카드도 라인스코어처럼
"경기가 여기까지 어떻게 왔나"를 말하는 자리가 된다. 그러려면 타순이 맞아야 하고,
그래서 21번의 수정이 선행 조건이었다.

### 26. 이닝은 역순인데 이닝 안은 시간순이다

문자중계가 마흔 행이 넘어가면서 순서를 정해야 했다. 두 질문이 다르다.

- 화면을 열었을 때 → **"지금 무슨 일이 났나"**. 최신 이닝이 위여야 한다
- 지난 이닝을 펼쳤을 때 → **"그 이닝이 어떻게 흘렀나"**. 흐름을 거꾸로 읽으면
  안타보다 그 주자를 불러들인 적시타가 먼저 나온다

한 규칙으로 통일하는 게 깔끔해 보이지만, 그러면 둘 중 하나는 반드시 어색해진다.

접힌 머리에 그 이닝 득점을 적어 두는 것이 이 구조의 값어치다 - 펼치지 않고도
`7회초 1점 · 6회말 1점 · 5회초 2점 · 3회말 2점` 이 세로로 읽히고, 그게 곧 경기 흐름이다.

### 27. 진행 중인 타석에 결과를 적고 있었다

타입체크와 검증 스크립트를 전부 통과한 뒤 화면에서 잡았다.

```
채은성   고의4구 · 0볼 0스트라이크      ← 지금 예측 중인 타석
```

바로 위 카드가 "채은성의 출루 확률 34%"를 계산하는 동안 **중계는 답을 먼저 말하고
있었다.** 피드가 `logLine`(그 타석에서 실제로 일어난 일)을 진행 중인 행에도 그대로
쓴 탓이다. 값의 정합은 맞고 의미가 틀린 종류라, 세는 검사로는 절대 안 잡힌다.

### 28. 표를 산 뒤가 통째로 비어 있었다

직관 탭은 **예매하기 전**까지를 맡는다(언제 열리나·얼마인가·어디 대나). 그런데 예매를
마치고 나면 앱 안에 그 사실이 아무 데도 남지 않아, 팬은 그때부터 문자함을 뒤진다.

MY 맨 위에 `내 티켓`을 뒀다. 다른 카드는 나에 대한 것이라 급할 것이 없지만
표는 **경기 당일에 앱을 여는 이유 그 자체**다.

예매 내역만 다시 보여 주는 것은 문자 메시지가 이미 한다. 앱이 더 할 수 있는 것은
**그 표에 딸린 결정**이다 - 몇 시에 나갈지(당일 알림), 어느 문으로 들어갈지
(좌석 등급 → 게이트), 경기 전후에 어디서 먹을지(조기 예매 쿠폰).

⚠ 바코드는 결정적인 막대 패턴이고 발권하지 않는다. 그럴듯한 코드를 그리면 **게이트에서
찍힐 것처럼 보인다.** 카드에 그 사실을 한 줄로 적어 둔다.

### 29. 조기 예매 쿠폰 - 선예매권과 반드시 구분한다

구단은 좌석을 **일찍** 채워야 하고(잔여 예측·인력·식음 발주가 거기 걸린다), 후원의 집은
경기 전후 손님이 필요하다. 조기 예매 쿠폰이 둘을 한 번에 푼다 - 구단은 현금을 쓰지 않고
제휴 상권 할인을 얹고, 가게는 확정된 손님을 받는다.

**한화는 암표 대응으로 등급별 선예매를 폐지했다.** 그 결정을 되돌리자는 제안이 아니라는
것이 이 기능의 유일한 전제다. 이건 '먼저 살 권리'가 아니라 **이미 열린 표를 일찍 정한
사람에게 주는 동네 혜택**이라 판매 순서를 건드리지 않는다. 구분이 흐려지면 폐지한 제도를
다시 들이는 제안으로 읽히므로, 쿠폰 시트가 그 문장을 직접 적는다.

문턱은 **7일·3일 둘뿐**이다. 세 단계 이상이면 팬이 표를 사면서 계산을 해야 하는데,
그건 이 정도 혜택이 요구할 만한 수고가 아니다.

쿠폰을 못 받은 표에는 "놓쳤습니다"라고 쓰지 않는다. 자기 표를 여는 자리에서 지적을 받는
꼴이라, **다음에 어떻게 하면 되는지**만 적는다.

### 30. 목록과 달력은 두 화면이 아니라 두 질문이다

- 목록: "다음 경기가 언제지" - 순서대로 읽힌다
- 달력: "이번 달 언제 갈 수 있지" - 주말이 어디 있는지, 원정 구간이 며칠인지는
  **격자로 놓여야 보인다**. 목록으로 그 질문에 답하려면 열일곱 줄을 세어야 한다

목록은 **다음 여섯 경기**만 세운다. 열일곱 줄을 다 펼치면 아래 예매 카드가 화면 두 개만큼
밀려나 이 화면의 동선이 끊긴다. 잘라낸 것을 숨기지 않고 마지막 행에 적고, 그 행이 곧
달력을 여는 문이다.

⚠ 달력 칸의 선택 표시는 **테두리**다. 칸의 글자가 10pt 인데 토큰 규칙상 오렌지 면 위
흰 글자는 14pt 굵게 이상에서만 허용된다.

⚠ 요일 머리글에 빨강을 쓰지 않았다. 달력 관습으로는 일요일이 빨강이지만 이 앱에서
빨강은 '진행 중'이다. **관습을 따르려고 상태색을 빌리면 그 색이 두 가지를 뜻하게 된다.**

---

## 다음에 볼 것

- **화면마다 리듬이 하나뿐이다** - 모든 탭이 `작은 회색 머리글 + 흰 카드`만 반복한다
  (홈 5회, 라이브 6회, MY 3회). "가기 전에" 세 타일이 좋아 보이는 이유는 아이콘이 아니라
  **그 구간만 리듬이 다르기 때문**이다. 아이콘을 다른 탭에 흩뿌리는 것으로는 풀리지 않는다
- 새로 들어온 `infoTileLabel` 이 `fontSize: 13, fontWeight: '700'` 을 직접 쓰고,
  `infoHint` 는 **S2 에서 버린 굵기 500** 을 쓴다. 굵기 누수가 재발하고 있다
- `Button` 을 쓰는 화면이 아직 없다 - 온보딩 '다음'은 명백히 `primary` 자리인데
  `brandSoft` 면이라 지면과 구분이 약하다
- 굿즈 상품 이미지 부재 - 회전 전시가 엠블럼으로 대체 중
- '설명 깊이'·'계산' 섹션 - 설정값이지 지면이 아니다. 컨셉 시연 때문에 남긴 자리
- `Button` 을 쓰는 화면이 아직 없다 - primary 를 어디 놓을지는 디자인 판단
- 굿즈 상품 이미지 부재 - 회전 전시가 엠블럼으로 대체 중
